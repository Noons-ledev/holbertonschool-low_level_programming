#include "main.h"
/**
 *mul- multiplie deux entiers
 *@a @b: Characters to multiply
 *Description: Does what mul does
 *Return: Product
 */
int mul(int a, int b)
{
int produit = a * b;
return (produit);
}
