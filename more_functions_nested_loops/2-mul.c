#include "main.h"
/**
 *mul- multiplie deux entiers
 *@b: Characters to multiply
 *@a: second character
 *Description: Does what mul does
 *Return: Product
 */
int mul(int a, int b)
{
int produit = a * b;
return (produit);
}
