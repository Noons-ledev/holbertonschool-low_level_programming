#include "main.h"
/**
* _islower- Checks for lower or uppercase
*@b: Character to check
* Description : Checks for lower or uppercase
* Return: Always return 1 for lowercase (true)
*/

int _islower(int b)
{
return (b >= 'a' && b <= 'z');
}
