#include "main.h"
/**
 *add- additionne deux chiffres donnes
 *@a @b: integers to add
 *description : On entre deux chiffre
 *Return: Always return 0(success)
 */

int add(int a, int b)
{
int res = a + b;
return (res);
}
