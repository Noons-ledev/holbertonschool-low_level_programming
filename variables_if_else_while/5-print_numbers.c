#include <stdio.h>

/**
 *main -Entry of program
 *Description: Print numbers from 0 to 9
 *Return: Always return 0(Success)
 */
int main(void)
{
int n;
for (n = 0; n <= 9; n++)
{
printf ("%d\n", n);
}
return (0);
}
