#include <stdio.h>
#include <stdlib.h>
/**
 *
 *
 *
 *
 */
int main(void)
{
  int i = 0;

  for (i = 1; i < 90; i++)
    {
      int a = i / 10;
      int b = i % 10;

      if ( a != b || a < b)
	{
	  putchar(a);
	    putchar(b);
	}
      
